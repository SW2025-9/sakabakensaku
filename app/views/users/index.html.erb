<% @users.each do |f| %>
  <p>
      uid: <%= f.uid %>, 
      password_digest: <%=f.password_digest %>
      <%= link_to '削除', user_path(f), data: {turbo_method: :delete} %>
  </p>
<% end %>
<p><%= link_to '新規追加', new_user_path %></p>
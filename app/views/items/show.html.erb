<h2><%= @item.name %></h2>

<% if @item.image.attached? %>
  <%= image_tag @item.image, style: "max-width: 300px;" %>
<% else %>
  <p>画像がありません</p>
<% end %>

<p>価格：<%= @item.price %>円</p>
<p>カテゴリー：<%= @item.category %></p>
<p>説明：<%= @item.detail %></p>
